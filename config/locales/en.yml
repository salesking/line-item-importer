en:
  activerecord:
    attributes:
      attachment:
        col_sep: Column separator
        encoding: Encoding
        quote_char: Quote character
      data_row:
        log: Log
        sk_id: SalesKing ID
        source: Source data
      import:
        col_sep: Column Separator
        created_at: Created at
        kind: Data type
        quote_char: Quote character
      mapping:
        document_id: You can choose to add line items to an existing draft document by choosing it from a search below. Keep this field empty, if you want to create a new document.
        document_type: So, what documents would you like to work with?
        enum_values:
          document_type:
            credit_note: Credit note
            estimate: Estimate
            invoice: Invoice
            order: Order
          import_type:
            document: I want to create one document for each row
            line_item: I want to import all rows into one document (existing or new)
        import_type: ! 'First of all, choose how do you want to import your data:'
      mapping_element:
        conv_opts: Convert options
        conv_type: Convert type
        source: Source field
        target: SalesKing field
    models:
      attachment:
        one: Uploaded file
        other: Uploaded files
      data_row:
        one: Data row
        other: Data rows
      import:
        one: Import
        other: Imports
      mapping:
        one: Field mapping
        other: Field mappings
  attachments:
    csv_import_error: Try with different 'Column Separator' and/or 'Quote character'.
    delete_confirm: This is an irreversible process. This will delete associated imports. Are you sure?
    new:
      can_use_tab_character_html: You can use <tt>\t</tt> to designate tabulators.
      enable_javascript: Please enable JavaScript to use file uploader.
      only_for_advanced_users: This setting is only for advanced users. Leave it, if you don't know what we're talking about.
      page_title: Upload new CSV file
      preview: CSV preview
      preview_info: This is just a preview. It shows only first three rows of the document. If you continue, this app will import all rows to SalesKing.
    proceed_to_mapping: Proceed to mapping
    upload: Upload
  data_rows:
    failed: Failed rows
    success: Successfull rows
  errors:
    sk_login_required: Please login to SalesKing.eu and navigate to "Settings" > "Apps" to activate.
  imports:
    create: Import data
    new: Create import
    title_failed:
      one: ! '%{count} row failed'
      other: ! '%{count} rows failed'
    title_success:
      one: ! '%{count} row imported'
      other: ! '%{count} rows imported'
    total:
      one: Total %{count} row
      other: Total %{count} rows
  layouts:
    application:
      attachments: Datasets
      imports: Imports
      mappings: Mappings
  link:
    actions: Actions
    back_to_list: Back to list
    confirm_destroy: Really delete this record?
    destroy: Delete
    edit: Edit
    revert: Revert
    show: Show
    step: ! 'Step %{count} from %{total}:'
  mappings:
    dnd_info: Please drag&drop fields above onto their targets below. Drop multiple fields on a target to join values (space-delimited) e.g for the tag list.
    form:
      document_id_placeholder: Type here to search through available draft documents
      document_mapping: Target document fields
      field_mappings: Field mapping settings
      line_item_mapping: Target line item fields
      line_item_mapping_info: At least price_single and quantity must be mapped to a source field.
      proceed_to_import: Proceed to import
      reuse_this_mapping_button: Reuse selected field mapping
      setup: Initial setup
      source_fields_info: If your source lacks fields to match some document-/line-item fields, you might want to edit your csv to add fields and their content and then upload it again.
    new: Create field mapping
    reuse: Select an existing mapping from the list to reuse that or create a new mapping in the below section.
    source_fields: Source fields
    target_fields: SalesKing fields
    title:
      one: ! '%{count} field: %{fields}'
      other: ! '%{count} fields: %{fields}'
  time:
    am: am
    formats:
      date_only: ! '%A, %m.%d.%Y'
      default: ! '%m.%d.%Y, %H:%M'
      long: ! '%B %d, %Y %H:%M'
      month_year: ! '%B, %Y'
      short: ! '%d %b %H:%M'
      shorter: ! '%m.%d.%y %H:%M'
      time: ! '%H:%M'
    pm: pm
